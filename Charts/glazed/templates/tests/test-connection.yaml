apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glazed.fullname" . }}-test-connection"
  labels:
    {{- include "glazed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glazed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
