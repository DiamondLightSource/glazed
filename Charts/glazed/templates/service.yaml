apiVersion: v1
kind: Service
metadata:
  name: {{ include "glazed.fullname" . }}
  labels:
    {{- include "glazed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glazed.selectorLabels" . | nindent 4 }}
