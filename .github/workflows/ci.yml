name: Glazed CI

on:
  push:
    branches:
      - main
  pull_request:


jobs:
  rust:
    uses: ./.github/_rust.yml
  
  container:
    uses: ./.github/_container.yml
  
  helm:
    uses: ./.github/_helm.yml